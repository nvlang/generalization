# Everything in this sub-project is generated by `lake build GeneralizationLinter:docs` except the
# three files listed below. Ignoring by default and re-including those is deliberate: an earlier
# enumerated list (doc/, doc-data/, lake-manifest.json) already missed `api-docs.db`, and doc-gen4
# gains output files from time to time.
*
!.gitignore
!lakefile.toml
!lean-toolchain
