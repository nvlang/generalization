# Everything here is generated by `lake build GeneralizationLinter:docs`; only `lakefile.toml`
# is tracked. Kept local to the sub-project rather than in the root .gitignore.
doc/
doc-data/
lake-manifest.json
